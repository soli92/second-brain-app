import { HttpParams } from "@angular/common/http";

export enum NerdyApiGwResources {
    BOOKS = 'books',
    MOVIES = 'movies',
    MUSIC = 'music',
    TV_SERIES = 'tv-series',
    USERS = 'users'
}