import { OverlayPanelDirective } from './overlay-panel.directive';

describe('OverlayPanelDirective', () => {
  it('should create an instance', () => {
    const directive = new OverlayPanelDirective();
    expect(directive).toBeTruthy();
  });
});
