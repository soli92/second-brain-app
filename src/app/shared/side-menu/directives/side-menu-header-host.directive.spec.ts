import { SideMenuHeaderHostDirective } from './side-menu-header-host.directive';

describe('SideMenuHeaderHostDirective', () => {
  it('should create an instance', () => {
    const directive = new SideMenuHeaderHostDirective();
    expect(directive).toBeTruthy();
  });
});
